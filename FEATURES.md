# Easy-Link 기능 명세서

Easy-Link는 시니어 사용자가 복잡한 공공서비스 웹페이지를 쉽게 이해할 수 있도록 돕는 서비스입니다. 핵심 기능은 다음 4가지입니다.

## 1. URL 기반 자동 파싱 (크롤링)

- 사용자가 웹페이지 주소(URL)만 입력하면, 복사·붙여넣기 없이 페이지 내용을 자동으로 가져옵니다.
- 광고, 메뉴, 스크립트 등 불필요한 부분은 제거하고 핵심 본문 텍스트만 추출합니다.
- 내부 시스템 주소나 존재하지 않는 페이지 등 잘못된 요청은 안전하게 차단하고, 상황에 맞는 안내 메시지를 보여줍니다.

## 2. AI 기반 3단계 안내문 생성

- 추출된 텍스트를 AI가 분석해 어려운 행정 용어를 일상적인 쉬운 말로 바꿔줍니다.
- 결과는 항상 **[1단계 → 2단계 → 3단계]** 형태로 정리되어, "무엇을 어떻게 해야 하는지" 순서대로 알려줍니다.
- 원문에 없는 내용은 지어내지 않고, 정보가 부족하면 관련 기관에 문의하라고 솔직하게 안내합니다.
- 무료 AI 모델을 사용하며, 일시적으로 응답이 지연되거나 실패해도 자동으로 다시 시도합니다.

## 3. 시니어 친화적 UI / 접근성(a11y)

- 고대비 색상과 큰 글씨를 기본으로 적용해 눈이 편안하고 잘 보이도록 설계했습니다.
- 입력창, 버튼 등 주요 요소는 크고 명확한 레이블을 가지고 있어 조작이 쉽습니다.
- 키보드나 스크린리더로도 이용할 수 있도록 오류 안내, 포커스 이동 등 접근성 기준을 지켰습니다.
- 오류가 발생하면 이유를 알기 쉬운 한국어 문장으로 알려줍니다.

## 4. 음성 안내 (TTS)

- 생성된 안내문을 버튼 한 번으로 소리 내어 읽어줍니다.
- 별도 프로그램 설치 없이 브라우저 자체 기능(Web Speech API)을 사용합니다.
- 음성 기능을 지원하지 않는 브라우저에서는 자동으로 안내 문구를 보여주고 텍스트로만 이용할 수 있습니다.
